[package]
name = "chained-codec"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.10.1"
tokio-util = { version = "0.7.15", features = ["codec"] }

[features]
default = ["encoder", "decoder"]
encoder = []
decoder = []
